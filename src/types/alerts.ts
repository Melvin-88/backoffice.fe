export enum AlertSeverity {
  high = 'high',
  medium = 'medium',
  low = 'low',
}

export enum AlertType {
  information = 'information',
  warning = 'warning',
  alert = 'alert',
  critical = 'critical',
}
